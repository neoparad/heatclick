{
  "name": "ugokimap-banner-runtime",
  "version": "0.1.0",
  "private": true,
  "description": "Phase 2A Banner Engine — Cloudflare Worker for edge banner decision (PRD §2 F1/F5)",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "node --import tsx --test --test-reporter spec test/worker.test.ts",
    "test:unit": "node --import tsx --test --test-reporter spec test/worker.test.ts",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260411.1",
    "tsx": "^4.19.0",
    "typescript": "^5.4.0",
    "wrangler": "^4.0.0"
  }
}
