{
  "name": "ugokimap-screenshot-worker",
  "version": "0.1.0",
  "private": true,
  "description": "Heatmap screenshot Worker — Puppeteer + autoScroll for lazy-loaded image capture",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@cloudflare/puppeteer": "^0.0.9"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260411.1",
    "typescript": "^5.4.0",
    "wrangler": "^4.0.0"
  }
}
